{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "voyage-planner-edge",
      "runtimeExecutable": "/bin/sh",
      "runtimeArgs": ["-c", "PATH=/opt/homebrew/bin:/usr/local/bin:$PATH exec npx vite --host --base / --port \"$PORT\" --strictPort"],
      "port": 8092,
      "autoPort": true
    }
  ]
}
